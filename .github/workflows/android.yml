name: Android CI/CD

on:
  push:
    branches: [ "master" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew build
      
    - name: Build Release APK
      run: ./gradlew assembleRelease
      
    - name: List APK files before signing
      run: |
        echo "Listing APK files in release directory:"
        ls -la app/build/outputs/apk/release/
      
    - name: Sign Release APK
      uses: r0adkll/sign-android-release@v1
      id: sign_app
      with:
        releaseDirectory: app/build/outputs/apk/release
        signingKeyBase64: ${{ secrets.SIGNING_KEY }}
        alias: ${{ secrets.KEY_ALIAS }}
        keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
        keyPassword: ${{ secrets.KEY_PASSWORD }}
        signedReleaseFile: app-release-signed.apk
      
    - name: Debug Signed APK Path
      run: |
        echo "Signed Release File: ${{ steps.sign_app.outputs.signedReleaseFile }}"
        echo "Listing files in release directory after signing:"
        ls -la app/build/outputs/apk/release/
        
    - name: Upload Release APK
      uses: actions/upload-artifact@v3
      with:
        name: app-release-signed
        path: ${{ steps.sign_app.outputs.signedReleaseFile }}
      
    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: ${{ steps.sign_app.outputs.signedReleaseFile }}
        name: Release ${{ github.ref_name }}
        draft: false
        prerelease: false
        body: |
          Release ${{ github.ref_name }}
          
          Changes in this release:
          - Automated build from master branch
          - See commit history for details
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
